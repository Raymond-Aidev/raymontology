# Raymontology Frontend Development Roadmap

**Total Estimated Time**: 40-48 시간 (약 2주)
**기준**: 각 단계 독립 테스트 가능, 2-3시간 이내 작업량

---

## 개발 로드맵 요약

```
Week 1: Foundation + Core UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Phase 1-4: 프로젝트 셋업 + 레이아웃 + API 연결 (8시간)
Phase 5-7: 메인 검색 + 자동완성 (6시간)
Phase 8-10: 관계도 그래프 기본 (8시간)

Week 2: Features + Polish
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Phase 11-13: 노드 상세 패널 (6시간)
Phase 14-16: 분석 보고서 페이지 (6시간)
Phase 17-19: Re-center + 네비게이션 (6시간)
Phase 20-22: 기간 필터 + 최적화 (6시간)
```

---

## Phase 1: 프로젝트 셋업 (1시간)

### 구현 내용
- [x] Vite + React + TypeScript 프로젝트 생성
- [x] 필수 패키지 설치
- [x] 프로젝트 폴더 구조 생성
- [x] ESLint + Prettier 설정

### 명령어
```bash
npm create vite@latest raymontology-frontend -- --template react-ts
cd raymontology-frontend
npm install react-router-dom zustand @tanstack/react-query axios d3 tailwindcss
npm install -D @types/d3 autoprefixer postcss
npx tailwindcss init -p
```

### 필요한 파일
```
src/
├── main.tsx
├── App.tsx
├── index.css (Tailwind imports)
├── api/
├── components/
├── hooks/
├── pages/
├── store/
└── types/
```

### 검증 방법
```bash
npm run dev
# ✓ 체크포인트: localhost:5173 접속, React 기본 화면 표시
```

---

## Phase 2: Tailwind CSS + shadcn/ui 설정 (1시간)

### 구현 내용
- [x] Tailwind CSS 설정
- [x] shadcn/ui 초기화
- [x] 기본 컴포넌트 설치 (Button, Input, Card)
- [x] 전역 스타일 설정

### 명령어
```bash
npx shadcn-ui@latest init
npx shadcn-ui@latest add button input card
```

### 필요한 파일
```
├── tailwind.config.js (업데이트)
├── src/index.css (업데이트)
├── src/lib/utils.ts
└── src/components/ui/
    ├── button.tsx
    ├── input.tsx
    └── card.tsx
```

### 검증 방법
```tsx
// App.tsx에 추가
import { Button } from '@/components/ui/button'
<Button>Test Button</Button>
# ✓ 체크포인트: shadcn 버튼이 올바른 스타일로 렌더링됨
```

---

## Phase 3: 라우팅 설정 (1.5시간)

### 구현 내용
- [x] React Router 설정
- [x] 4개 페이지 라우트 생성
- [x] 레이아웃 컴포넌트 생성
- [x] 404 페이지

### 필요한 컴포넌트
```
src/
├── App.tsx (Router 설정)
├── layouts/
│   └── MainLayout.tsx
├── pages/
│   ├── LoginPage.tsx (placeholder)
│   ├── MainSearchPage.tsx (placeholder)
│   ├── GraphPage.tsx (placeholder)
│   ├── ReportPage.tsx (placeholder)
│   └── NotFoundPage.tsx
```

### 라우트 구조
```tsx
<Routes>
  <Route path="/login" element={<LoginPage />} />
  <Route element={<MainLayout />}>
    <Route path="/" element={<MainSearchPage />} />
    <Route path="/company/:companyId/graph" element={<GraphPage />} />
    <Route path="/company/:companyId/report" element={<ReportPage />} />
  </Route>
  <Route path="*" element={<NotFoundPage />} />
</Routes>
```

### 검증 방법
```
# ✓ 체크포인트:
- / → MainSearchPage 표시
- /company/test/graph → GraphPage 표시
- /company/test/report → ReportPage 표시
- /login → LoginPage 표시
- /xyz → 404 페이지 표시
```

---

## Phase 4: API 클라이언트 + 타입 정의 (2시간)

### 구현 내용
- [x] Axios 클라이언트 설정
- [x] 모든 API 타입 정의
- [x] API 함수 작성
- [x] React Query Provider 설정

### 필요한 파일
```
src/
├── api/
│   ├── client.ts          # Axios 인스턴스
│   ├── report.ts          # /api/report/*
│   └── graph.ts           # /api/graph/*
├── types/
│   ├── api.ts             # API 응답 타입
│   ├── graph.ts           # 그래프 타입
│   └── company.ts         # 회사 타입
└── main.tsx               # QueryClientProvider 추가
```

### API 연동
| 함수 | 엔드포인트 |
|-----|-----------|
| `searchCompanies(q)` | `GET /api/report/search?q=` |
| `getCompanyReport(name)` | `GET /api/report/name/{name}` |
| `getHighRiskCompanies()` | `GET /api/report/high-risk` |
| `getCompanyGraph(id)` | `GET /api/graph/company/{id}` |

### 검증 방법
```tsx
// 임시 테스트 코드
const { data } = useQuery({
  queryKey: ['search', '삼성'],
  queryFn: () => searchCompanies('삼성')
});
console.log(data);
# ✓ 체크포인트: 콘솔에 API 응답 데이터 출력
```

---

## Phase 5: Header 컴포넌트 (1.5시간)

### 구현 내용
- [x] Header 레이아웃
- [x] 로고
- [x] 상단 검색창 (나중에 자동완성 연결)
- [x] 사용자 메뉴 (placeholder)

### 필요한 컴포넌트
```
src/components/common/
├── Header.tsx
└── Logo.tsx
```

### UI 명세
```
┌─────────────────────────────────────────────────────────────┐
│  [Logo]    [────────검색창────────]    [User Menu ▼]       │
└─────────────────────────────────────────────────────────────┘
```

### 검증 방법
```
# ✓ 체크포인트:
- Header가 모든 페이지 상단에 고정 표시
- 검색창 입력 가능 (아직 동작 안 함)
- 반응형: 모바일에서 검색창 축소
```

---

## Phase 6: 메인 검색 페이지 UI (2시간)

### 구현 내용
- [x] 중앙 검색 영역
- [x] 큰 검색 입력창
- [x] 배경 그라데이션/일러스트
- [x] 고위험 회사 카드 섹션 (placeholder)

### 필요한 컴포넌트
```
src/pages/MainSearchPage.tsx
src/components/common/
├── SearchBar.tsx (기본 UI만)
└── CompanyCard.tsx
```

### UI 명세
```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    Raymontology                             │
│              관계형 리스크 분석 서비스                        │
│                                                             │
│        ┌────────────────────────────────────┐              │
│        │  회사명을 입력하세요...              │              │
│        └────────────────────────────────────┘              │
│                                                             │
│   [고위험 회사]                                              │
│   ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                         │
│   │ A사 │ │ B사 │ │ C사 │ │ D사 │                         │
│   └─────┘ └─────┘ └─────┘ └─────┘                         │
└─────────────────────────────────────────────────────────────┘
```

### 검증 방법
```
# ✓ 체크포인트:
- 메인 페이지에 큰 검색창 표시
- 검색창에 텍스트 입력 가능
- 모바일 반응형 레이아웃
```

---

## Phase 7: 자동완성 검색 기능 (2.5시간)

### 구현 내용
- [x] 자동완성 드롭다운 UI
- [x] Debounce 검색 (300ms)
- [x] 키보드 네비게이션 (↑↓ Enter)
- [x] 회사 선택 시 관계도 페이지 이동

### 필요한 컴포넌트/훅
```
src/components/common/SearchBar.tsx (업데이트)
src/hooks/useCompanySearch.ts
```

### API 연동
| Hook | API |
|------|-----|
| `useCompanySearch(query)` | `GET /api/report/search?q={query}` |

### 검증 방법
```
# ✓ 체크포인트:
- "삼성" 입력 → 0.3초 후 드롭다운에 결과 표시
- 결과에 회사명, CB건수, 리스크등급 표시
- ↓↑ 키로 항목 선택 가능
- Enter 또는 클릭 → /company/{id}/graph 이동
```

---

## Phase 8: 그래프 페이지 레이아웃 (1.5시간)

### 구현 내용
- [x] 3단 레이아웃 (헤더 / 그래프 / 사이드패널)
- [x] 그래프 영역 placeholder
- [x] 사이드 패널 토글
- [x] 기간 선택기 placeholder
- [x] [RaymondsRisk] 버튼

### 필요한 컴포넌트
```
src/pages/GraphPage.tsx
src/components/graph/
├── GraphContainer.tsx (placeholder)
└── GraphControls.tsx
src/components/panels/
└── SidePanel.tsx (placeholder)
```

### UI 명세
```
┌─────────────────────────────────────────────────────────────┐
│  Header                                                      │
├─────────────────────────────────────────┬───────────────────┤
│                                         │                   │
│                                         │   Side Panel      │
│           Graph Area                    │   (닫힘 가능)     │
│           (placeholder)                 │                   │
│                                         │                   │
├─────────────────────────────────────────┴───────────────────┤
│  [2024.01 ~ 2025.Q2 ▼]           [RaymondsRisk 분석보고서]  │
└─────────────────────────────────────────────────────────────┘
```

### 검증 방법
```
# ✓ 체크포인트:
- /company/test/graph 접속 시 레이아웃 표시
- 사이드 패널 열기/닫기 동작
- [RaymondsRisk] 버튼 클릭 → /company/test/report 이동
```

---

## Phase 9: D3.js 그래프 기본 구현 (3시간)

### 구현 내용
- [x] D3 Force Simulation 설정
- [x] SVG 캔버스 생성
- [x] 노드 렌더링 (원형)
- [x] 엣지 렌더링 (선)
- [x] 줌/패닝 기능

### 필요한 컴포넌트
```
src/components/graph/
├── NetworkGraph.tsx       # 메인 그래프 컴포넌트
├── GraphNode.tsx          # 개별 노드
└── GraphEdge.tsx          # 개별 엣지
```

### D3 설정
```typescript
const simulation = d3.forceSimulation(nodes)
  .force('charge', d3.forceManyBody().strength(-300))
  .force('link', d3.forceLink(edges).distance(100))
  .force('center', d3.forceCenter(width/2, height/2))
  .force('collision', d3.forceCollide().radius(30));
```

### 검증 방법 (Mock 데이터)
```typescript
// Mock 데이터로 테스트
const mockNodes = [
  { id: '1', type: 'Company', name: '테스트회사' },
  { id: '2', type: 'Officer', name: '홍길동' },
];
const mockEdges = [
  { source: '1', target: '2' }
];
# ✓ 체크포인트:
- 노드 2개와 연결선 1개 표시
- 드래그로 노드 위치 조정 가능
- 마우스 휠로 줌 인/아웃 가능
```

---

## Phase 10: 그래프 API 연동 (2.5시간)

### 구현 내용
- [x] 회사 그래프 데이터 로드
- [x] 노드 타입별 색상 적용
- [x] 노드 클릭 이벤트
- [x] 로딩/에러 상태 처리

### API 연동
| Hook | API |
|------|-----|
| `useCompanyGraph(companyId)` | `GET /api/graph/company/{id}` |

### 필요한 훅
```
src/hooks/useGraphData.ts
```

### 검증 방법
```
# 실제 API 테스트
1. 검색 → "엑시온그룹" 선택
2. 관계도 페이지 이동
# ✓ 체크포인트:
- 중심에 엑시온그룹 노드 (파란색)
- 연결된 임원 노드들 (초록색)
- 연결된 CB 인수인 노드들 (보라색)
- 노드 클릭 시 콘솔에 노드 정보 출력
```

---

## Phase 11: 그래프 범례 + 툴팁 (1.5시간)

### 구현 내용
- [x] 노드 타입별 범례
- [x] 노드 호버 툴팁
- [x] 위험 노드 표시 (붉은색 테두리)

### 필요한 컴포넌트
```
src/components/graph/
├── GraphLegend.tsx
└── NodeTooltip.tsx
```

### 범례 UI
```
┌─────────────────────┐
│ ● 회사 (중심)       │
│ ● 회사 (연결)       │
│ ● 임원              │
│ ● 대주주            │
│ ● CB 인수인         │
│ ● 계열사            │
│ ━━━━━━━━━━━━━━━━    │
│ ◉ 위험 표시 (타사이력)│
└─────────────────────┘
```

### 검증 방법
```
# ✓ 체크포인트:
- 그래프 좌측 하단에 범례 표시
- 노드 호버 시 이름/타입 툴팁 표시
- 타사 이력 있는 노드는 붉은색 테두리
```

---

## Phase 12: 사이드 패널 - 임원 상세 (2시간)

### 구현 내용
- [x] 임원 노드 클릭 시 패널 표시
- [x] 기본 정보 (이름, 직위, 임기)
- [x] 타사 근무 이력 목록
- [x] 상장사 데이터 있으면 붉은색 표시

### API 연동
| Hook | API |
|------|-----|
| `useOfficerCareer(officerId)` | `GET /api/graph/officer/{id}/career` |

### 필요한 컴포넌트
```
src/components/panels/
├── SidePanel.tsx (업데이트)
├── OfficerDetail.tsx
└── CompanyCard.tsx
```

### UI 명세
```
┌─────────────────────────┐
│ 임원 정보        [X]    │
├─────────────────────────┤
│ 홍길동                  │
│ 대표이사                │
│ 2024.01 ~ 2027.01       │
├─────────────────────────┤
│ 타사 근무 이력          │
│                         │
│ ┌─────────────────────┐│
│ │ A회사 (상장)     🔴 ││
│ │ 이사 2020~2023      ││
│ └─────────────────────┘│
│ ┌─────────────────────┐│
│ │ B회사              ││
│ │ 감사 2018~2020      ││
│ └─────────────────────┘│
└─────────────────────────┘
```

### 검증 방법
```
# ✓ 체크포인트:
- 임원 노드 클릭 → 사이드 패널에 상세 정보 표시
- 타사 근무 이력 목록 표시
- 상장사+데이터 있는 회사는 붉은색 표시
```

---

## Phase 13: 사이드 패널 - CB 인수인 상세 (2시간)

### 구현 내용
- [x] CB 인수인 노드 클릭 시 패널 표시
- [x] 총 투자액 표시
- [x] 다른 회사 CB 참여 이력
- [x] 클릭 가능한 회사 카드

### API 연동
| Hook | API |
|------|-----|
| `useSubscriberInvestments(id)` | `GET /api/graph/subscriber/{id}/investments` |

### 필요한 컴포넌트
```
src/components/panels/
└── SubscriberDetail.tsx
```

### 검증 방법
```
# ✓ 체크포인트:
- CB 인수인 노드 클릭 → 사이드 패널에 투자 이력 표시
- 총 투자액 계산 표시
- 각 회사별 투자 금액/일자 표시
```

---

## Phase 14: 분석 보고서 페이지 레이아웃 (1.5시간)

### 구현 내용
- [x] 페이지 레이아웃
- [x] 헤더 (회사명, 기본정보)
- [x] 탭 네비게이션
- [x] [관계도 보기] 버튼

### 필요한 컴포넌트
```
src/pages/ReportPage.tsx
src/components/report/
├── ReportHeader.tsx
└── ReportTabs.tsx
```

### 탭 구조
```
[리스크] [CB 발행] [임원] [재무] [주주] [계열사]
```

### 검증 방법
```
# ✓ 체크포인트:
- /company/test/report 접속 시 레이아웃 표시
- 탭 클릭 시 활성 탭 변경
- [관계도 보기] 클릭 → /company/test/graph 이동
```

---

## Phase 15: 리스크 대시보드 (2.5시간)

### 구현 내용
- [x] 종합 점수 게이지 차트
- [x] 투자등급 배지
- [x] 리스크 레벨 표시
- [x] 점수 구성 차트 (인적/CB/재무)
- [x] 리스크 신호 목록

### API 연동
| Hook | API |
|------|-----|
| `useCompanyReport(companyId)` | `GET /api/report/name/{name}` |

### 필요한 컴포넌트
```
src/components/report/
├── RiskDashboard.tsx
├── RiskGauge.tsx (@visx 사용)
├── RiskBreakdown.tsx
└── RiskSignalList.tsx
```

### UI 명세
```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   종합 리스크 점수                                           │
│   ┌─────────────┐  ┌──────┐ ┌──────────┐                   │
│   │   43.7      │  │  BB  │ │  MEDIUM  │                   │
│   │  ────●────  │  └──────┘ └──────────┘                   │
│   │  0    100   │                                          │
│   └─────────────┘                                          │
│                                                             │
│   점수 구성                                                  │
│   ├── 인적 리스크 ████░░░░░░ 4.9점                          │
│   ├── CB 리스크  ██████████ 50.0점                         │
│   └── 재무건전성 ██████░░░░ 60.0점                          │
│                                                             │
│   리스크 신호                                                │
│   ┌─────────────────────────────────────────┐              │
│   │ [HIGH] 반복적 CB 발행 패턴              │              │
│   │ 90일 이내 반복 CB 발행 8회 감지          │              │
│   └─────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────┘
```

### 검증 방법
```
# 엑시온그룹으로 테스트
# ✓ 체크포인트:
- 종합 점수 43.7점 표시
- 투자등급 BB 표시
- 점수 구성 바 차트 표시
- 리스크 신호 2건 표시
```

---

## Phase 16: 세부 데이터 탭 (2시간)

### 구현 내용
- [x] CB 발행 테이블
- [x] CB 인수인 테이블
- [x] 임원 현황 테이블
- [x] 재무제표 테이블
- [x] 주주 테이블
- [x] 계열회사 테이블

### 필요한 컴포넌트
```
src/components/report/
├── CBTable.tsx
├── SubscriberTable.tsx
├── OfficerTable.tsx
├── FinancialTable.tsx
├── ShareholderTable.tsx
└── AffiliateTable.tsx
```

### 검증 방법
```
# ✓ 체크포인트:
- 각 탭 클릭 시 해당 테이블 표시
- 데이터 정렬/페이지네이션 동작
- 금액은 억원 단위로 포맷팅
```

---

## Phase 17: Re-center 기능 (2시간)

### 구현 내용
- [x] 노드 더블클릭 → 중심 전환
- [x] 사이드 패널 회사 카드 클릭 → 중심 전환
- [x] URL 파라미터 업데이트
- [x] 그래프 애니메이션 전환

### API 연동
| 함수 | API |
|------|-----|
| `recenterGraph(nodeType, nodeId)` | `POST /api/graph/recenter` |

### 필요한 훅
```
src/hooks/useGraphNavigation.ts
```

### 검증 방법
```
# ✓ 체크포인트:
1. 임원 노드 클릭 → 사이드 패널
2. 타사 회사 클릭 (붉은색)
3. 새 회사 중심으로 그래프 재구성
4. URL이 /company/{newId}/graph로 변경
```

---

## Phase 18: 네비게이션 히스토리 (2시간)

### 구현 내용
- [x] 히스토리 스택 관리
- [x] 뒤로가기/앞으로가기 버튼
- [x] 브라우저 히스토리 연동
- [x] 히스토리 브레드크럼 표시

### 필요한 컴포넌트
```
src/components/graph/
├── NavigationButtons.tsx
└── Breadcrumb.tsx
```

### UI 명세
```
[← 뒤로] [앞으로 →]    삼성전자 > 홍길동(임원) > A회사
```

### 검증 방법
```
# ✓ 체크포인트:
- 여러 회사 탐색 후 [뒤로] 클릭 → 이전 회사 관계도
- 브라우저 뒤로가기 버튼도 동일하게 동작
- 브레드크럼에 탐색 경로 표시
```

---

## Phase 19: 기간 필터 구현 (2시간)

### 구현 내용
- [x] DateRangePicker 컴포넌트
- [x] 프리셋 버튼 (최근1년, 2년, 전체)
- [x] 기간 변경 시 그래프 재로드
- [x] URL 쿼리 파라미터 연동

### 필요한 컴포넌트
```
src/components/common/
└── DateRangePicker.tsx
```

### 검증 방법
```
# ✓ 체크포인트:
- 기간 선택 후 [적용] → 그래프 재로드
- 프리셋 버튼 클릭 → 해당 기간 자동 설정
- URL에 ?start=2024-01-01&end=2025-06-30 반영
```

---

## Phase 20: Zustand 스토어 최적화 (2시간)

### 구현 내용
- [x] graphStore 완성
- [x] reportStore 완성
- [x] authStore 완성
- [x] 스토어 persist (localStorage)

### 필요한 파일
```
src/store/
├── graphStore.ts
├── reportStore.ts
└── authStore.ts
```

### 검증 방법
```
# ✓ 체크포인트:
- 페이지 새로고침 후 상태 유지
- DevTools에서 스토어 상태 확인 가능
```

---

## Phase 21: 로딩/에러 상태 (1.5시간)

### 구현 내용
- [x] 로딩 스켈레톤 UI
- [x] 에러 바운더리
- [x] 네트워크 에러 토스트
- [x] 빈 데이터 상태

### 필요한 컴포넌트
```
src/components/common/
├── Loading.tsx
├── Skeleton.tsx
├── ErrorBoundary.tsx
└── EmptyState.tsx
```

### 검증 방법
```
# 네트워크 탭에서 Slow 3G 설정
# ✓ 체크포인트:
- 데이터 로딩 중 스켈레톤 표시
- API 에러 시 에러 메시지 표시
- 데이터 없을 때 빈 상태 표시
```

---

## Phase 22: 성능 최적화 + 마무리 (2.5시간)

### 구현 내용
- [x] React.memo 적용
- [x] 그래프 노드 수 제한 (200개)
- [x] 프리페칭 설정
- [x] 이미지/폰트 최적화
- [x] Lighthouse 점수 확인

### 최적화 포인트
```typescript
// 노드 수 제한
const MAX_NODES = 200;
const filteredNodes = nodes.slice(0, MAX_NODES);

// 프리페칭
queryClient.prefetchQuery(['graph', companyId], ...);

// 메모이제이션
const MemoizedNode = React.memo(GraphNode);
```

### 검증 방법
```bash
npm run build
npm run preview
# Lighthouse 성능 점수 확인
# ✓ 체크포인트:
- Performance > 80
- Accessibility > 90
- 그래프 60fps 유지
```

---

## 의존성 그래프

```
Phase 1 ─────┬─> Phase 2 ─> Phase 3 ─┬─> Phase 4
             │                       │
             │                       ↓
             │     Phase 5 ─> Phase 6 ─> Phase 7
             │                       │
             ↓                       ↓
Phase 8 ─> Phase 9 ─> Phase 10 ─> Phase 11 ─> Phase 12 ─> Phase 13
             │                       │
             ↓                       ↓
Phase 14 ─> Phase 15 ─> Phase 16    Phase 17 ─> Phase 18 ─> Phase 19
             │                                       │
             ↓                                       ↓
         Phase 20 ──────────────────────────────> Phase 21 ─> Phase 22
```

---

## 체크리스트 요약

### Week 1
- [ ] Phase 1: 프로젝트 셋업
- [ ] Phase 2: Tailwind + shadcn
- [ ] Phase 3: 라우팅
- [ ] Phase 4: API 클라이언트
- [ ] Phase 5: Header
- [ ] Phase 6: 메인 검색 UI
- [ ] Phase 7: 자동완성
- [ ] Phase 8: 그래프 레이아웃
- [ ] Phase 9: D3 그래프 기본
- [ ] Phase 10: 그래프 API 연동

### Week 2
- [ ] Phase 11: 범례 + 툴팁
- [ ] Phase 12: 임원 상세 패널
- [ ] Phase 13: 인수인 상세 패널
- [ ] Phase 14: 보고서 레이아웃
- [ ] Phase 15: 리스크 대시보드
- [ ] Phase 16: 세부 데이터 탭
- [ ] Phase 17: Re-center
- [ ] Phase 18: 네비게이션 히스토리
- [ ] Phase 19: 기간 필터
- [ ] Phase 20: 스토어 최적화
- [ ] Phase 21: 로딩/에러 상태
- [ ] Phase 22: 성능 최적화
